# Exposes the MinIO S3 API externally so CI (techdocs-cli publish) can push
# built docs. The operator creates a Service named "minio" (port 80 -> 9000
# when requestAutoCert is false). Backstage itself reads via the in-cluster
# service and does NOT need this route.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: minio-s3
  namespace: minio
  annotations:
    external-dns.alpha.kubernetes.io/managed: "true"
    external-dns.alpha.kubernetes.io/hostname: s3.bergtobias.com
    # Point at the Cloudflare tunnel (proxied CNAME) instead of letting
    # external-dns derive an A record to the private node IP, which Cloudflare
    # rejects for proxied records.
    external-dns.alpha.kubernetes.io/target: 07a7a2df-49c5-41a5-8e59-6403b236a5d5.cfargotunnel.com
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: traefik
      sectionName: websecure
  hostnames:
    - s3.bergtobias.com
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: minio
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
