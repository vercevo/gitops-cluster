server:
  standalone:
    enabled: true
    config: |
      ui = true

      listener "tcp" {
        tls_disable     = 1
        address         = "[::]:8200"
        cluster_address = "[::]:8201"
      }

      storage "file" {
        path = "/vault/data"
      }

      # For production, replace file storage with auto-unseal:
      # seal "awskms" {
      #   region     = "us-east-1"
      #   kms_key_id = "alias/vault-unseal"
      # }
      # And use storage "raft" or "consul" for HA.

  dataStorage:
    enabled: true
    size: 10Gi
    storageClass: local-path  # k3s default (rancher/local-path-provisioner)

  ingress:
    enabled: true
    ingressClassName: traefik
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/managed: "true"
    hosts:
      - host: vault.k8s.bergtobias.com
        paths:
          - /
    tls:
      - secretName: vault-tls
        hosts:
          - vault.k8s.bergtobias.com

ui:
  enabled: true
  serviceType: ClusterIP
