global:
  domain: argocd.bergtobias.com

# Per-component metrics + ServiceMonitor (CRD from kube-prometheus-stack).
controller:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
repoServer:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
applicationSet:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

server:
  ingress:
    enabled: false
  extraArgs:
    - --insecure
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

configs:
  params:
    server.insecure: "true"

  cm:
    users.anonymous.enabled: "false"
    application.resourceTrackingMethod: annotation
    url: https://argocd.bergtobias.com
    oidc.config: |
      name: Authentik
      issuer: https://authentik.bergtobias.com/application/o/argocd/
      clientID: argocd
      clientSecret: $oidc.authentik.clientSecret
      requestedScopes:
        - openid
        - profile
        - email

  rbac:
    policy.default: role:readonly
    policy.csv: |
      g, akadmin, role:admin
